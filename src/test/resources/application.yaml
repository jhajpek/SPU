spring:
  config:
    activate:
      on-profile: test
      
  application:
    name: spu
    
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test_db
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  sql:
    init:
      mode: never

  docker:
    compose:
      enabled: false